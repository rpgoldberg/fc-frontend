# API Configuration
# When using nginx proxy (Docker/Coolify): /api
# For local React dev server with backend proxy: /api (default)
# For direct backend access (bypassing proxy): http://localhost:5000/api
REACT_APP_API_URL=/api

# Optional: Direct backend URL (for debugging, not typically needed)
# REACT_APP_BACKEND_URL=http://localhost:5000

# Debug Logging (optional)
# Enable debug output in browser console
# REACT_APP_DEBUG=true
# REACT_APP_DEBUG_LEVEL=verbose
# REACT_APP_DEBUG_MODULES=auth,api
